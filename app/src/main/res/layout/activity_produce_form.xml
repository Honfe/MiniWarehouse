<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/producePartsItemText"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="@string/produce_parts_item_title"
            android:textSize="20sp"
            android:textColor="@color/black"
            android:background="@color/textBackgroud"
            android:gravity="center" />

        <TextView
            android:id="@+id/produceProductItemText"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="@string/produce_product_item_title"
            android:textSize="20sp"
            android:textColor="@color/black"
            android:background="@color/textBackgroud"
            android:gravity="center"/>

    </LinearLayout>

    <ImageView
        android:id="@+id/produceLineImg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="matrix"
        android:src="@mipmap/line"
        tools:ignore="ContentDescription" />

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/producePager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="10dp" />

</LinearLayout>